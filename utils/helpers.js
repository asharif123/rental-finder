module.exports = {
    log: function(foo) {
        // where the actual data is
        // console.log(foo[0].listing[0].listing);
        console.log(foo);
        },
        
  

      
        
}

